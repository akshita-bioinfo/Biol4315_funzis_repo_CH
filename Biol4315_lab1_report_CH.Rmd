---
title: "bio3415 lab-1 Report"

author: "Catherine"
date: "2026-09-14"
output: html_document
---
Partner: Akshita

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Vegan package
```{r, eval=FALSE}
install.packages("vegan")
```

# Text ggplot2
Next, we will load the **ggplot2** package to create a data visualization. Using the built-in `cars` dataset, we will generate a scatter plot to explore the relationship between vehicle speed (`speed`) and stopping distance (`dist`).

# Code ggplot2
```{r}
library(ggplot2)

ggplot(data = cars, aes(x = speed, y = dist)) +
  geom_point() +
  labs(
    title = "Vehicle Speed vs. Stopping Distance",
    x = "Speed (mph)",
    y = "Stopping Distance (ft)"
  )
```

