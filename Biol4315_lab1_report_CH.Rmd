---
title: "Biol4315 Lab-1 Report"
author: "Catherine"
date: "2026-09-15"
output: html_document
---
## Partner
Akshita

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

## Adding the vegan package

```{r, eval=FALSE}
install.packages("vegan")
```

## ggplot2 
Next, we will load the **ggplot** package to create  data visualization. Using the build-in 'cars' dataset, we will generate a scatter plot to explore the relationshp between vehicle speed ('speed') and stopping distance ('dist')

# Create a scatter plot 

```{r, eval=FALSE}
library(ggplot2)

ggplot(data = cars, aes(x = speed, y = dist)) +
  geom_point(color = "blue", size = 2) +
  labs(
    title = "Vehicle Speed vs. Stopping Distance",
    x = "Speed (mph)",
    y = "Stopping Distance (ft)"
  ) +
  theme_minimal()
```







